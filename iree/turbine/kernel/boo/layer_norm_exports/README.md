## Minimalistic support for generating and launching layer normalization kernels.

Sample usage:

```
TURBINE_LOG_LEVEL=DEBUG BOO_CACHE_ON=1 BOO_CACHE_DIR=$PWD/tmp python boo_driver.py  --sizes=4,32,42,32
```
